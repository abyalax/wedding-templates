pre-commit:
  commands:
    lint:
      glob:
        - "**/*.{js,ts,jsx,tsx,json,d.ts}"
        - "!build/**"
        - "!dist/**"
      run: pnpm lint && pnpm format
      stage_fixed: true

    build:
      run: pnpm build
